export const cellSizePx = 32;

export const debug = {
  enabled: false,
  cellColor: [255, 255, 255, 0.2],
};

export const editor = {
  brushes: { ice: "i", snow: "s", water: "w" },
  defaultTile: "ssss",
  brush: "i",
  brushSize: 1,
};
